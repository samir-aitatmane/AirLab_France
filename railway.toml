[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"

[cron]
schedule = "0 16 * * *"
command = "python ingest.py"
```

**3. `Dockerfile`** — pas obligatoire avec Railway (il détecte automatiquement Python), tu peux le laisser vide ou le supprimer.

**4. `.gitignore`** — vérifie qu'il contient bien :
```
.env
__pycache__/
*.pyc
venv/
data/
*.duckdb
logs/